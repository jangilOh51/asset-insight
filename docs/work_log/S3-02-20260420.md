# S3-02 작업 로그 — 투자 전략서 페이지

> 작업일: 2026-04-20
> 작업자: Claude/PM, Claude/기획, Claude/UX, Claude/백엔드, Claude/프론트, Claude/리뷰어, Claude/QA
> 상태: 완료

---

## [기획자] 기능 명세

### 작업 내용
- `docs/plans/S3-02-strategy-page.md` 작성
- `docs/screens/screen-10-strategy.md` 작성

### AC 목록
- AC-01: POST /api/v1/report/strategy — 리스크 성향 + 목표 기간 → 전략서 생성
- AC-02: 동일 파라미터+포트폴리오 구성 요청 → Redis 캐시(2h) 반환
- AC-03: 시스템 프롬프트 프롬프트 캐싱 적용
- AC-04: `/strategy` 페이지 5가지 UI 상태
- AC-05: 전략서 7개 섹션 (진단/전략/실행계획/리스크/조정안/주의사항/면책)

### PM 검토
✅ 승인 — AC 5개 명확, 7섹션 구조가 월간 리포트(5섹션)와 차별화됨. 캐시 키에 포트폴리오 해시 포함하여 포트폴리오 변경 시 자동 무효화 설계가 적절함.

---

## [UX] 디자인

### 작업 내용
- 리스크 성향: 3-button toggle (보수적/중립적/공격적)
- 투자 기간: 4-button toggle (1/3/5/10년)
- 선택 상태: `#06B6D4` 배경, 미선택: `#1A2332`
- 면책 섹션: 별도 색상(`#4B5563`)으로 시각적 구분
- screen-10-strategy.md에 5가지 상태 정의

### PM 검토
✅ 승인 — 디자인 토큰 준수. 면책 섹션 시각적 차별화로 사용자가 AI 생성물의 한계를 명확히 인지 가능. toggle 버튼 UX가 직관적.

---

## [백엔드] 구현

### 작업 내용
1. `backend/app/services/analysis/report.py` 리팩터링
   - `_call_claude()` 공통 헬퍼로 월간/전략서 코드 중복 제거
   - `generate_strategy_report()` 추가 (7섹션 시스템 프롬프트, 프롬프트 캐싱)
   - `_portfolio_hash()` 유틸 추가
   - `RiskLevel` 타입 export
2. `backend/app/api/v1/endpoints/report.py` 확장
   - `_fetch_portfolio()` 공통 헬퍼 분리 (월간/전략서 공유)
   - `POST /api/v1/report/strategy` 엔드포인트 추가
   - 캐시 키: `report:strategy:{risk_level}:{horizon_years}:{portfolio_hash}` (2h TTL)

### 변경 파일
- `backend/app/services/analysis/report.py` (리팩터링 + 기능 추가)
- `backend/app/api/v1/endpoints/report.py` (기능 추가)

### PM 검토
✅ 승인 — `_call_claude()` 공통화로 중복 제거됨. 캐시 키에 포트폴리오 해시 포함 설계 일관. 코드 리뷰어 MUST(미사용 import) 즉시 수정 완료.

---

## [프론트] 구현

### 작업 내용
1. `frontend/types/index.ts` — `RiskLevel`, `StrategyReport` 타입 추가
2. `frontend/lib/api.ts` — `fetchStrategyReport()` 추가
3. `frontend/pages/strategy/index.tsx` — 전략서 페이지 신규 생성
4. `frontend/components/layout/AppLayout.tsx` — `/strategy` "투자 전략서" nav 항목 추가

### 5가지 UI 상태 구현
| 상태 | 구현 |
|------|------|
| 기본 | ✅ 설정 카드 + 안내 문구 |
| 로딩 | ✅ 스피너 + "AI가 전략서를 작성 중입니다..." |
| 에러 | ✅ 에러 카드 + 다시 시도 버튼 |
| 성공 | ✅ 7섹션 카드 파싱, 면책 섹션 별도 스타일 |
| 빈 화면 | ✅ 초기 진입 안내 |

### PM 검토
✅ 승인 — 5가지 상태 모두 구현, 디자인 토큰 일관 사용, TypeScript strict 준수. 면책 섹션 색상 처리가 UX 요구사항과 일치.

---

## [코드 리뷰어] 검토

**MUST**
- `backend/app/api/v1/endpoints/report.py` — `Literal` 미사용 import → 제거 완료

**NIT**
- `hashlib`은 엔드포인트 내 `_portfolio_hash` 헬퍼가 사용하므로 유지

### PM 검토
✅ 승인 — MUST 항목 수정 완료. NIT 항목 확인: `hashlib`은 `_portfolio_hash()` 함수에서 실제 사용 중이므로 올바른 판단.

---

## [QA] 테스트

### 전략서 신규 테스트 4개 (test_report.py 누적 10개)

| # | 테스트명 | 검증 내용 |
|---|---------|---------|
| 7 | test_create_strategy_report_success | 캐시 미스 → 200, risk_level/horizon_years 반환, Redis.setex 호출 |
| 8 | test_create_strategy_report_cache_hit | 캐시 히트 → generate_strategy_report 미호출 |
| 9 | test_create_strategy_report_invalid_risk_level | unknown risk → 422 |
| 10 | test_generate_strategy_report_calls_claude | 시스템 프롬프트에 "중립적"/"3년" 포함 검증 |

### AC 커버리지
- AC-01 (전략서 생성): test 7 ✅
- AC-02 (Redis 캐시): test 8 ✅
- AC-03 (프롬프트 캐싱): 서비스 코드 검토 ✅
- AC-04 (5가지 UI 상태): 코드 검토 ✅
- AC-05 (7섹션): test 10 시스템 프롬프트 내용 간접 검증 ✅

### PM 검토
✅ 승인 — invalid risk_level 422 검증 포함. 서비스 레이어 프롬프트 파라미터 검증(한국어 리스크 레이블 변환)이 test 10에서 확인됨.

---

## [PM] 최종 완료 선언

- 완료 일시: 2026-04-20

| 항목 | 결과 |
|------|------|
| CLAUDE.md 컨벤션 준수 | ✅ |
| TypeScript 타입 에러 | ✅ any 없음, RiskLevel 타입 분리 |
| 새 엔드포인트 단위 테스트 | ✅ 4개 (누적 10개) |
| PLAN.md 업데이트 | ✅ |
| 영향받은 문서 업데이트 | ✅ screen-10, S3-02 계획서, 작업 로그 |

### 특이사항
- 월간 리포트 서비스 리팩터링으로 `_call_claude()` 공통화 — 전략서 추가에 따른 기술 부채 선제 해소
- 코드 리뷰어 MUST 항목 1건(미사용 import) 즉시 수정, PM 로그 확인 후 승인
